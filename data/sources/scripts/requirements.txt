# All four extractor scripts use only the Python 3.11 stdlib:
#   extract_cma.py / extract_met.py / extract_smithsonian_fsg.py / download_images.py
# (json, csv, urllib.request, concurrent.futures, pathlib, re, datetime, argparse).
# No third-party packages required.

# Optional, only if you swap json.load for streaming parsers on the 325 MB CMA dump:
# ijson>=3.2
